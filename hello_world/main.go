package main

import "fmt"

func main() {
	fmt.Printf("Hello, world!\n")
	// Código de formateo

	// gofmt -d hello.go, te muestra que partes va a formatear, pero no hace los cambios.
	// gofmt -w hello.go, formatea el código
}
